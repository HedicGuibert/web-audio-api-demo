import handleForm from '../public/js/form/handleForm.js';
import addPresetSongs from '../public/js/presetSongs/presetSongs.js';
window.addEventListener('DOMContentLoaded', () => {
    addPresetSongs();
    handleForm();
});
